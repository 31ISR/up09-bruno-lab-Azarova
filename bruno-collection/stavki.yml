info:
  name: stavki
  type: http
  seq: 8

http:
  method: POST
  url: "{{url}}/items/:id/bids"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
